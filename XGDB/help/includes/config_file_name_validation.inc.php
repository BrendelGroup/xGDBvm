<div class="dialogcontainer">
	<h2 class="bottommargin1">File Name Validation</h2>
	<p> For Status=<span class="Development">Development</span>, the configuration page displays a list of <b>valid filenames</b> contained within your Input Data Directory and <b>expected output tracks</b> e.g.: </p>

	<ul class="bullet1 indent2">
        <li>1) <span class="heading">Input</span> <span class="checked"> 4 valid files totalling 200 MB:</span> --  (filename / date / size )</li>
        <li>2) <span class="warning">permissions problems </span> (filename) </li>
        <li>3) <span class="heading">Expected Output</span> <span style="color:orange; font-weight:bold"> 3 tracks based on valid inputs  </span>  -- (track names, colors)</li>
	</ul>
	<p>If some of your files do not  pass validation, rename or fix their permissions them according to xGDB's <a target="_blank" title="Detailed file format and naming conventions (click ? to open new web page)" href="/XGDB/help/create_gdb.php#config_file_names">filename requirements</a> and then re-visit the validation box</p>
	<p>For Status=<span class="Current">Current</span>, the page displays an <b>archive</b> of the valid files that were present when the GDB was created. </p>
	<p>In 
	    <span class="Update">
	        Update
	    </span>
	    mode, the configuration page shows a list of 
	        <span class="checked">
	            valid files
	        </span>
	        in the Update Data Directory and expected 
	    <span style="color: cyan; font-weight: bold">
	        Update Actions
	    </span>
	</p>
		<span class="heading normalfont linkback"> <a class="smallerfont" href="/XGDB/help/create_gdb.php#config_name_validation">View this in Help Context</a> (create_gdb.php/config_name_validation)</span>
</div>
