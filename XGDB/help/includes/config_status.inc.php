<div class="dialogcontainer">
		<h3>Status</h3>
	<ul class="bullet1">
		<li>For each GDB configuration, its <b>Status</b> is displayed at the top of the Config page:
			<ul>				 					
				<li>GDB Status is <span class="Development">Development</span> until you run the GDB pipeline or restore from Archive (accessed under "Data Process Options...")</li>
				<li>GDB Status changes to <span class="Locked">Locked</span> while the pipeline is running or Restore is underway</li>
				<li>GDBD Status changes to <span class="Current">Current</span> when the process is complete and the data display is live</li>
				<li>GDBD Status can be changed to <span class="Offline">Offline</span> if you no longer want to display it in the main menu</li>
			</ul>
		</li>
	</ul>
<span class="heading normalfont linkback"> <a class="smallerfont" href="/XGDB/help/create_gdb.php#config_status">View this in Help Context</a> (create_gdb.php/config_status)</span>

</div>
